= form_tag user_profile_path do
  
  #posts
    #post_box
      %table
        %td.field
          = text_area_tag :wall_post,params[:wall_post], :cols => "25", :rows => "3"

        %td.actions
          = submit_tag 'Submit', :name => 'submit_button'
    
    %table
      - @posts.each do |apost|     
        %tr
          %td= link_to apost.user.username, "localhost:3001/users/#{apost.id}/profile"
        %tr
          %td=apost.wall_post


