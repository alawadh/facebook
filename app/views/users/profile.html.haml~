%h2 form4 is loaded if the profile we're viewing is on the logged in user's friends, otherwise form 5 is loaded

%h4=link_to 'facebook', user_newsfeed_path

#prof_header
    %p
      %h1= "#{@user.first_name} #{@user.last_name}"
    %p
      %h2.profile= "Username: #{@user.username}"

- if @flag
  = render 'form4'
- else
  = render 'form5'
  
%footer
  - if current_user.id == @user.id
    = link_to 'Profile Preferences', user_path
  
